root = "."
tmp_dir = "tmp"

[build]
# Trỏ air vào đúng file main tại thư mục cmd/api
cmd = "go build -o ./tmp/main ./cmd/api"
bin = "./tmp/main"
include_ext = ["go", "tpl", "tmpl", "html"]
exclude_dir = ["assets", "tmp", "vendor"]
delay = 500

[log]
time = true
